import Comparison from '../comparison';
import FabricModal from './fabric-modal';
import WarpModal from './warp-modal';

const Modal = () => {
  return <Comparison name="Modal" Fabric={FabricModal} Warp={WarpModal} />
}

export default Modal;
